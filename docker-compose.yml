services:
  postgres:
    image: postgres:14-alpine
    container_name: crudUsuario-postgres
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - crudUsuario-postgres:/var/lib/postgresql/data

volumes:
  crudUsuario-postgres:
    name: crudUsuario_postgres